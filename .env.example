# Domain and URL Configuration
DOMAIN_NAME=demo.rybbit.com
BASE_URL="https://${DOMAIN_NAME}"

# Authentication and Security
BETTER_AUTH_SECRET=insecure-secret
DISABLE_SIGNUP=false

# Webserver Configuration
# Set to false to disable the built-in Caddy webserver
USE_WEBSERVER=true
# Host port mappings - use "hostPort:containerPort" or "hostIp:hostPort:containerPort"
# Examples: 3001:3001 (bind all) or 127.0.0.1:3001:3001 (localhost only)
HOST_BACKEND_PORT=3001:3001
HOST_CLIENT_PORT=3002:3002

# Telemetry (optional)
DISABLE_TELEMETRY=false

# ClickHouse Database Configuration
# Override CLICKHOUSE_HOST on Dokploy if backend can't resolve "clickhouse" (e.g. http://<project>-clickhouse-1:8123)
CLICKHOUSE_HOST=http://clickhouse:8123
CLICKHOUSE_DB=analytics
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=frog

# PostgreSQL Database Configuration
# Override POSTGRES_HOST on Dokploy if backend can't resolve "postgres" (e.g. <project>-postgres-1)
POSTGRES_HOST=postgres
POSTGRES_DB=analytics
POSTGRES_USER=frog
POSTGRES_PASSWORD=frog

# External Service Tokens (optional)
MAPBOX_TOKEN=
